1. When an account is created - select country code for phone - then enter phone. If +359 is selected for BG validate the number by symbols count if it includes a 0 at the begining
2. Password field in SignUp form doesn't work with cyrilic letters
3. Write log functionality or print statements (to be desided) to follow and find out from all the functions and variables, which are not used!
4. /admin is not ideal for production. Use a non-obvious admin path, for example /internal-control-panel. Consider IP whitlistin or VPN-only admin access. Add rate limiting on login/admin endpoints. Require HTTPS before public use.
5. When a cleaner registers, confirms the email and then is approved from the frontend admin panel it still needs to be approved from backend adminpanel in Cleaner Profiles section
6. When a cleaner have finished a job for a host, only then a host can leave a reference for that cleaner if he wishes to.



// Use CSS-only animation instead of adding Framer Motion or another animation library. //


3582